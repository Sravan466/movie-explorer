{
  "name": "movie-explorer",
  "version": "1.0.0",
  "description": "A full-stack movie exploration application",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
} 